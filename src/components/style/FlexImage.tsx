import styled from "styled-components";
import { motion } from "framer-motion";

export const FlexImage = styled(motion.img)`
  width: 570px;
  height: 100%;
  cursor: pointer;
  object-fit: scale-down;
`;
