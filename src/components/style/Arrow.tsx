import styled from "styled-components";
import { motion } from "framer-motion";

export const Arrow = styled(motion.img)`
  width: 38px;
  height: 38px;
  filter: invert(94%) sepia(0%) saturate(0%) hue-rotate(206deg) brightness(105%)
    contrast(108%);
  background: transparent;
`;
